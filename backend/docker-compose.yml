version: "3.8"

services:
  backend:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.mock
    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"
